/* global test */
const initialWolfState = {
  abilityCompleted: false,
  activeAbility: '',
  waitingFor: {
    slotName: null,
    turnCount: 0
  },
  messageQueue: [],
  pendingData: {}
}

type PizzaData = {
  pizzaType: string,
  pizzaSize: string,
  quantity: string  
}

describe('Add a Pizza a cart', () => { // Feature (ability)
  
})
