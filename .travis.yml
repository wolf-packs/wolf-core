language: node_js
node_js:
  - "node"
  - "8"
  - "7"

stages:
  - test
  - name: deploy
    if: branch = master AND tag = true

jobs:
  include:
    - stage: test
      script: npm test
    - stage: npm release
      node_js: "node"
      script:
        - npm run build
        - touch ~/.npmrc
        - echo "//registry.npmjs.org/:_authToken=$NPM_PUBLISH_AUTH_TOKEN" >> ~/.npmrc
        - npm publish
